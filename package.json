{
  "name": "migmate",
  "displayName": "MigMate",
  "description": "A prototype plugin that enables automated Python library migration.",
  "license": "SEE LICENSE IN LICENSE",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.101.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "python", "library migration", "migration", "refactoring", "llm", "automation"
  ],
  "pricing": "Free",
  "icon": "128x128",
  "repository": {
    "type": "git",
    "url": "https://github.com/sanadlab/MigMate.git"
  },
  "homepage": "https://github.com/sanadlab/MigMate/blob/main/README.md",




  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:python",
    "onLanguage:pip-requirements",
    "workspaceContains:**/requirements*.txt",
    "workspaceContains:pyproject.toml"
  ],
  "contributes": {
    "commands": [
      {
        "command": "migmate.migrate",
        "title": "Migrate a Library",
        "category": "MigMate"
      },
      {
        "command": "migmate.viewTestResults",
        "title": "View Migration Test Results",
        "category": "MigMate"
      },
      {
        "command": "migmate.setApiKey",
        "title": "Set API Keys to be Stored",
        "category": "MigMate"
      }
    ],
    "configuration": [
      {
        "title": "MigMate",
        "type": "object",
        "properties": {
          "migmate.flags.useCache": {
            "type": "boolean",
            "default": true,
            "order": 0,
            "description": "Use cache for the migration."
          },
          "migmate.flags.forceRerun": {
            "type": "boolean",
            "default": false,
            "order": 1,
            "description": "Forcefully rerun the migration."
          },
          "migmate.flags.smartSkipTests": {
            "type": "boolean",
            "default": false,
            "order": 2,
            "markdownDescription": "Skip running tests if they are already done. Tests are considered done if: \n\n1. No new files are migrated \n\n2. test-report.json file exists \n\n3. cov-report.json file exists"
          },
          "migmate.flags.maxFileCount": {
            "type": "integer",
            "default": 20,
            "minimum": 1,
            "order": 3,
            "description": "Maximum number of files to migrate. Abort the migration if more files are found."
          },
          "migmate.flags.pythonVersion": {
            "type": "string",
            "default": "3.12",
            "order": 4,
            "description": "Python version to use during migration."
          },
          "migmate.flags.repositoryName": {
            "type": "string",
            "default": "",
            "order": 5,
            "description": "Name of the repository. If blank, assumes the project directory name."
          },
          "migmate.flags.testRoot": {
            "type": "string",
            "default": "/tests",
            "order": 6,
            "description": "Path from where tests should be run. Should be relative to the project path."
          },
          "migmate.flags.outputPath": {
            "type": "string",
            "default": ".libmig",
            "order": 7,
            "description": "Output path for the migration. If relative, it is relative to the project path."
          },
          "migmate.flags.dependencyFilePath": {
            "type": "string",
            "default": "requirements.txt",
            "order": 8,
            "description": "Path to the main dependency file. If blank, assumes './requirements.txt'."
          },
          "migmate.flags.LLMClient": {
            "type": "string",
            "enum": [
              "gpt-4o-mini",
              "gpt-4o",
              "gpt-4.1"
            ],
            "default": "gpt-4o-mini",
            "order": 9,
            "description": "Select an LLM client to use for migration."
          },
          "migmate.options.showCliOutput": {
            "type": "boolean",
            "default": false,
            "order": 10,
            "description": "Show console output generated by the CLI tool."
          },
          "migmate.options.previewOnMigrationFailure": {
            "type": "boolean",
            "default": false,
            "order": 11,
            "description": "Show a preview for failed migrations."
          },
          "migmate.options.enableSuggestions (Experimental)": {
            "type": "boolean",
            "default": false,
            "order": 12,
            "description": "Suggest suitable target libraries by querying Libraries.io."
          },
          "migmate.options.previewStyle": {
            "type": "string",
            "enum": [
              "Webview",
              "Refactor Preview"
            ],
            "enumDescriptions": [
              "An experimental custom Webview",
              "VS Code's built-in Refactor Preview window"
            ],
            "default": "Webview",
            "order": 13,
            "description": "Choose how migrated files are shown during preview."
          }
        }
      }
    ],
    "icons": {
      "migmate-icon": {
        "description": "Cover icon for migmate view",
        "default": "library"
      }
    },
    "menus": {
      "editor/context": [
        {
          "when": "resourceFilename == requirements.txt || resourceFilename == requirements-dev.txt",
          "command": "migmate.migrate",
          "group": "navigation"
        }
      ]
    },
    "views": {
    },
    "viewsContainers": {
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "test:unit": "mocha -r ts-node/register src/test/unit/**/*.test.ts",
    "test:ext": "npm run compile && node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.101.0",
    "@typescript-eslint/eslint-plugin": "^8.28.0",
    "@typescript-eslint/parser": "^8.28.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2",
    "chai": "^4.5.0",
    "eslint": "^9.29.0",
    "mocha": "^11.7.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@vscode/extension-telemetry": "^1.0.0",
    "diff": "^8.0.2"
  }
}
